import 'package:flutter/material.dart';

class Pallete {
  // Primary colors - Dark Blue accents on White
  static const primaryColor = Color(0xFF1E3A5F);
  static const primaryLight = Color(0xFF4A90D9);
  static const primaryDark = Color(0xFF0D1B2A);

  // Button colors
  static const btnBackground = Color(0xFF1E3A5F);
  static const btnTextColor = Colors.white;
  static const logoutBtn = Color(0xFF4A90D9);

  // App bar
  static const appBarBackground = Colors.white;

  // Text colors
  static const textPrimary = Color(0xFF1E3A5F);
  static const textSecondary = Color(0xFF5A7A9A);
  static const textLink = Color(0xFF4A90D9);

  // Icon colors
  static const iconColor = Color(0xFF1E3A5F);
  static const iconSuccess = Color(0xFF4CAF50);

  // Card colors
  static const cardBackground = Color(0xFFF5F8FC);
  static const cardShadow = Color(0x201E3A5F);
  static const cardAccent = Color(0xFFE8F0F8);

  // Status colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFFA000);

  // Background colors
  static const scaffoldBackground = Colors.white;

  // Border colors
  static const border = Color(0xFFD0DCE8);

  // Category chip colors
  static const categoryChipBackground = Color(0xFFE8F0F8);
  static const categoryChipText = Color(0xFF1E3A5F);

  // Search bar
  static const searchBarBackground = Color(0xFFF5F8FC);
  static const searchBarHint = Color(0xFF8A9DB5);
}
