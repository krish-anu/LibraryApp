import 'package:flutter/material.dart';

class Pallete {
  // Primary colors
  static const primaryColor = Color(0xFF1B5E20);
  static const primaryLight = Color(0xFF4C8C4A);
  static const primaryDark = Color(0xFF0D2818);

  // Button colors
  static const btnBackground = Color.fromARGB(255, 7, 60, 9);
  static const btnTextColor = Colors.white;
  static const logoutBtn = Colors.white60;

  // App bar
  static const appBarBackground = Color(0xFF0D2818);

  // Text colors
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFF9E9E9E);
  static const textLink = Color(0xFFD32F2F);

  // Icon colors
  static const iconColor = Colors.white;
  static const iconSuccess = Color(0xFF4CAF50);

  // Card colors
  static const cardBackground = Color(0xFF1A3D2E);
  static const cardShadow = Colors.black26;
  static const cardAccent = Color(0xFF2D5A45);

  // Status colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFD32F2F);
  static const warning = Color(0xFFFFA000);

  // Background colors
  static const scaffoldBackground = Color(0xFF0D2818);

  // Border colors
  static const border = Color(0xFF2D5A45);

  // Category chip colors
  static const categoryChipBackground = Color(0xFF1A3D2E);
  static const categoryChipText = Colors.white;

  // Search bar
  static const searchBarBackground = Color(0xFF1A3D2E);
  static const searchBarHint = Color(0xFF7A9A8A);
}
