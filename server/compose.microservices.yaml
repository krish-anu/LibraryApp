services:
  db:
    image: postgres:16-alpine
    container_name: libraryapp-db
    environment:
      POSTGRES_DB: ${DB_NAME:-libraryapp}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-libraryapp}"]
      interval: 10s
      timeout: 5s
      retries: 5

  auth-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libraryapp-auth-service
    env_file:
      - .env
    environment:
      DATABASE_URL: ""
      DB_HOST: db
      DB_PORT: 5432
      DB_SSLMODE: disable
    command: uvicorn services.auth_service.main:app --host 0.0.0.0 --port 8000
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8101:8000"
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  catalog-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libraryapp-catalog-service
    env_file:
      - .env
    environment:
      DATABASE_URL: ""
      DB_HOST: db
      DB_PORT: 5432
      DB_SSLMODE: disable
    command: uvicorn services.catalog_service.main:app --host 0.0.0.0 --port 8000
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8102:8000"
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  users-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libraryapp-users-service
    env_file:
      - .env
    environment:
      DATABASE_URL: ""
      DB_HOST: db
      DB_PORT: 5432
      DB_SSLMODE: disable
    command: uvicorn services.users_service.main:app --host 0.0.0.0 --port 8000
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8103:8000"
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  circulation-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libraryapp-circulation-service
    env_file:
      - .env
    environment:
      DATABASE_URL: ""
      DB_HOST: db
      DB_PORT: 5432
      DB_SSLMODE: disable
    command: uvicorn services.circulation_service.main:app --host 0.0.0.0 --port 8000
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8104:8000"
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  settings-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: libraryapp-settings-service
    env_file:
      - .env
    environment:
      DATABASE_URL: ""
      DB_HOST: db
      DB_PORT: 5432
      DB_SSLMODE: disable
    command: uvicorn services.settings_service.main:app --host 0.0.0.0 --port 8000
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8105:8000"
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

volumes:
  postgres_data:
